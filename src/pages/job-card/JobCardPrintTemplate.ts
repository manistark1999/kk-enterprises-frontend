/**
 * Job Card Print HTML Template Generator
 * (Reserved for future use or legacy compatibility)
 */
export function generateJobCardPrintHTML(data: any, company?: any): string {
  return "<div>Job Card Print Template</div>";
}
